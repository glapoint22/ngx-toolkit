import { DynamicComponentCloseDirective } from './dynamic-component-close.directive';

describe('DynamicComponentCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicComponentCloseDirective();
    expect(directive).toBeTruthy();
  });
});
