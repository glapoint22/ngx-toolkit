import { PanelHeaderActionButtonDirective } from './panel-header-action-button.directive';

describe('PanelHeaderActionButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelHeaderActionButtonDirective();
    expect(directive).toBeTruthy();
  });
});
